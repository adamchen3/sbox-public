@using Sandbox;
@using Sandbox.UI;
@inherits Panel

@* Fills whatever it's in and fades the spinner in after a beat, so a quick load never flashes it. *@

<root>
    <Spinner class="large" />
</root>
